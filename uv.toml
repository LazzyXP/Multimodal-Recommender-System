# Keep the project lock reproducible across CI and developer machines. Override
# with `UV_INDEX_URL` or `uv ... --default-index` when a local mirror is needed.
[[index]]
name = "pypi"
url = "https://pypi.org/simple"
default = true
